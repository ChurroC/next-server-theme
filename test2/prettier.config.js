/** @type {import('prettier').Config & import('prettier-plugin-tailwindcss').PluginOptions} */
const config = {
  trailingComma: "none",
  tabWidth: 4,
  arrowParens: "avoid"
};


export default config;

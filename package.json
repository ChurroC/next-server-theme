{
  "name": "next-themes-compare",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "turbo start",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write .",
    "findMismatches": "syncpack list-mismatches",
    "fixMismatches": "syncpack fix-mismatches",
    "postinstall": "bun run findMismatches"
  },
  "devDependencies": {
    "turbo": "latest",
    "syncpack": "^12.3.2",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.14",
    "@repo/eslint-config": "workspace: *",
    "@repo/typescript-config": "workspace: *"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*",
    "apps/web",
    "apps/examples/*",
    "apps/comparison/*"
  ]
}

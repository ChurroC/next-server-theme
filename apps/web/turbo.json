{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "!src/tests/**", "!playwright/**"],
      "dependsOn": ["build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    }
  }
}
